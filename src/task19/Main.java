package task19;

public class Main {
    public static void main(String[] args) {
        Starter.startJustThread();
        Starter.startRunnableThread();
        Starter.startAnonymousThread();
        Starter.CallableThread();
    }
}